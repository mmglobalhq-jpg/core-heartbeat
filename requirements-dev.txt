# Dev-only dependencies (testing). Install with:
#   ./venv/bin/pip install -r requirements-dev.txt
# Includes the runtime stack plus the test toolchain.
# Note: httpx/httpcore are now RUNTIME deps (required by google-genai) and live
# in requirements.txt; the FastAPI TestClient reuses them.
-r requirements.txt

# Test framework and its pinned transitive deps
pytest==9.1.1
iniconfig==2.3.0
pluggy==1.6.0
pygments==2.20.0
