# Dev-only dependencies (testing). Install with:
#   ./venv/bin/pip install -r requirements-dev.txt
# Includes the runtime stack plus the test toolchain.
-r requirements.txt

# Test framework and its pinned transitive deps
pytest==9.1.1
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
pygments==2.20.0

# HTTP client powering FastAPI/Starlette's in-process TestClient (endpoint tests)
httpx==0.28.1
httpcore==1.0.9
certifi==2026.6.17
